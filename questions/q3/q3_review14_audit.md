# q3 review14 对照审计

来源：`questions/q3/review14.md`

## 总体判断

review14 合理。当前文档已经基本完成 Gate 2 后的可行性收口，但仍有两处需要在进入最终燃油优化代码实现前固定：一是 `approach.md` 的初始网格描述仍停留在 `N=31/61/121`；二是最终无风燃油优化的验收边界还没有形成可执行表格。

本轮只做文档收口，不运行最终燃油优化，也不把 Gate 2 可行轨迹写成最优轨迹。

## 处理记录

| review14 意见 | 处理状态 | 处理说明 |
|---|---|---|
| `approach.md` 初始网格描述略旧 | 已修正 | 改为 Gate 2 已完成 `N=31/61/121/241`，最终燃油优化建议按 `N=61 -> 121 -> 241` continuation |
| 下一阶段验收边界需更可执行 | 已补充 | `approach.md` 和 `results.md` 均新增最终无风燃油优化验收表 |
| 目标网格收敛阈值需预先固定 | 已补充 | 固定为 `abs(J_121-J_241)<=1 kg` 且相对变化 `<=1e-4` |
| 多初值目标差阈值需预先固定 | 已补充 | 固定为 `max(J)-min(J)<=1 kg` |
| 近零推力比例和时间比应报告但不硬判 | 已补充 | 近零推力比例只报告；`tf/t_base` 需报告并做 `1.05/1.10` 约束对照 |

## 下一步

下一步应进入代码实现和数值求解：

```text
实现并运行最终无风燃油优化：
目标 min(m0 - mf)；
删除质量松弛 s 或固定 s=0；
按 N=61 -> 121 -> 241 continuation；
输出最优结果表、验证表、目标网格收敛、多初值、时间约束对照、近零推力比例和 KKT/最优性诊断。
```

## 本轮结论

review14 的两项文档收口问题已处理。Q3 仍保持 `in_implementation`；Gate 2 可行性已通过，但正式无风燃油最优结果仍未生成。
