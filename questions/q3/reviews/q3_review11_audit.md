# q3 review11 对照审计

来源：`questions/q3/review11.md`

## 总体判断

review11 合理。review10 的 `N=31/61/121` 结果已经显示重积分误差按梯形法二阶趋势下降，因此本轮优先执行 `N=241`，并补齐 ODE 容差敏感性和沿程连续约束审计。

## 处理记录

| review11 意见 | 处理状态 | 处理说明 |
|---|---|---|
| 直接运行 `N=241`，预测速度误差约 `4.74e-4 m/s` | 已完成 | `no_wind_collocation_mesh_convergence.csv` 扩展到 `N=241`；实际速度误差约 `4.806e-4 m/s`，质量误差约 `0.01066 kg` |
| Gate 通过前需做 ODE 容差敏感性 | 已完成 | 新增 `no_wind_collocation_reintegration_tolerance.csv`，记录 `rtol=1e-8,1e-10,1e-12` 及对应严格 `atol` |
| 不能只看终端速度，需检查沿程连续误差和约束 | 已完成 | 新增 `no_wind_collocation_continuous_audit.csv`，记录最大状态重构误差和高度、速度、马赫、推力、航迹角连续约束违反 |
| Stage 1B 当前是备选，不应优先于 `N=241` | 已采纳 | 本轮未加入控制平滑目标；保持 Stage 1 一阶段可行性求解 |
| SLSQP 在 `N=241` 可能成为瓶颈，失败时不能放宽物理约束 | 已检查 | SLSQP 在 `N=241` 正常收敛，未放宽任何物理约束或 Gate 门槛 |
| 修正文档过时状态和 Q3-C08 局限 | 已完成 | `results.md` 改为“Gate 2 连续一致性已通过；正式燃油优化尚未开始”；`evidence.md` 与全局证据链修正 Q3-C08 局限 |

## 本轮结论

`N=241,h_max=12000 m` 的 Stage 1 可行性 Gate 已通过：质量松弛约 `5.89e-13 kg`，尺度化配点缺陷约 `5.97e-16`，重积分终端速度误差约 `4.806e-4 m/s`，连续约束违反为 `0`。

ODE 容差敏感性显示，`rtol=1e-8` 到 `1e-10` 的终端速度差异约 `3.22e-6 m/s`，`1e-10` 到 `1e-12` 的差异约 `4.55e-7 m/s`，均远低于 `1e-4 m/s` 检查口径。

该结果只说明 Gate 2 Stage 1 可行性门槛通过，可作为最终无风燃油优化的初值；尚不能写成最终最优油耗或最优轨迹。
