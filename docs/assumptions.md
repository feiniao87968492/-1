# 假设清单

| ID | 假设 | 理由 | 影响范围 | 如何验证 / 缓解 | 状态 |
|---|---|---|---|---|---|
| A01 | 第一问采用小航迹角准稳态巡航爬升解释，而非严格水平飞行 | 严格水平飞行无法产生高度变化；`tasks/overview.md` 给出闭合口径 | q1 模型、推导、代码、图表 | 在 `questions/q1/review.md` 中阻断确认；验证升力平衡和能量残差 | confirmed |
| A02 | q1 保持参考升力系数 `C_L=C_L_ref` 作为巡航爬升闭合条件 | 题目未给出最大推力、固定油门或剩余推力曲线，仅等速/等马赫不足以闭合；`CL_ref=0.658914` 接近 `sqrt(CD0/k)=0.699206` | q1 两种策略高度演化 | 与候选模型比较并做相对残差检查 | confirmed |
| A03 | 风速正值表示顺航向风，主模型采用 `W(h)=20+3e-5(h-10000)^2` | 当前 OCR 题面与用户最新指令均为该风场系数 | q1 航程和地速 | 做无风/有风对比，并在审计中说明与 `overview.md` 早期建议的差异 | confirmed_user_override |
| A04 | 主结果使用题设 `c_T=2.8e-4 kg/(N*s)`，另以 `2.8e-5` 做工程合理性对照 | 不静默修改题目参数，同时审计其数量级风险 | q1 时间尺度、耗油率、敏感性分析 | 在结果表标明参数口径，做敏感性分析 | mitigated |

## 规则

- 区分题目明确给定、常识性假设和为可计算性引入的假设。
- 对结果敏感的假设必须进入灵敏度分析。
- 被数据否定的假设不能静默保留，应标为 `rejected` 并调整模型。
