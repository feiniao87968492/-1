# 决策记录

| 日期 | 决策 ID | 问题 | 备选方案 | 最终决策 | 理由 | 影响 |
|---|---|---|---|---|---|---|
| 2026-07-06 | D001 | 仓库结构 | 单文件 / 分问题 | 分问题 + 共享模块 | 便于独立验证和复用 | 全项目 |
| 2026-07-06 | D002 | q1 进入代码前是否采用补齐题面和 `tasks/overview.md` 其余统一口径 | A 接受 / B 修改 / C 补题面后再审 / D 暂停 | 用户先选 C 补题面，再选 A 进入实现 | 题意存在关键多解，已完成代码前阻断确认 | q1 模型、代码、实验、证据链 |
| 2026-07-06 | D003 | q1 主风场公式 | OCR 题面 `20+0.00003(h-10000)^2` / overview 建议 `20+3e-6(h-10000)^2` / 用户确认 `20+3e-5(h-10000)^2` | 采用 `20+3e-5(h-10000)^2` | 用户明确指定统一修改风速大小，且与 OCR 题面一致 | q1 水平位移、地速、风场对比 |
| 2026-07-06 | D004 | q2 总油耗比较终止条件 | 固定终止质量 / 完整 q1 等速参考航程 / 共同可行航程 | 固定 q1 几何路径上的共同可行航程 `X_ref=189781.310 m` | 若固定终止质量，温度修正下总油耗恒定；若用完整 q1 航程，部分场景低于 `62000 kg`；共同航程可比较温差影响并满足终点质量下限 | q2 油耗积分和温度修正 |
| 2026-07-06 | D005 | q2 温度偏差处理方式 | 忽略 / 固定 ISA 压力简化 / 静力平衡常偏差修正 | 采用静力平衡常偏差修正，计算 `DeltaT in {-10,-5,-2,0,2,5,10} K` | 固定 ISA 压力简化不满足 `dp/dh=-rho g`；静力修正可同时更新压力、密度和声速，并支持温差灵敏度 | q2 结果、验证和图表 |
| 2026-07-06 | D006 | q2 路径与升力系数接口 | 沿用 q1 固定 `CL_ref` 反算高度 / 各场景重算初始 `CL_ref` / 固定 q1 几何路径并重算 `CL(x)` | 固定 q1 等速几何路径和速度路径，在各大气场景中重算 `CL(x)` | 避免 q1 指数密度 `CL_ref` 与 q2 ISA 密度不兼容，保证初始状态为 `h0=9500 m` 且路径低于 11 km | q2 主模型、验证和结果 |
| 2026-07-06 | D007 | q3 最优控制比较口径 | 固定终止质量 / 固定航程终端高度速度自由 / 固定航程并固定终端高度速度 | 固定航程 `Xf=189781.310 m`，固定 `h(tf)=10577.124 m` 与 `V(tf)=240 m/s`，最大化终端质量 | 避免油耗目标退化和降低终端机械能造成虚假省油 | q3 优化问题定义 |
| 2026-07-06 | D008 | q3 主求解路线 | 间接法 / 直接法 / 摄动法 | 直接配点作为主求解，PMP 作为必要条件诊断 | 直接法更适合处理状态/控制边界和变化率约束；PMP 用于校验最优性结构 | q3 后续数值实现 |
| 2026-07-07 | D009 | q3 无风求解器前基线口径 | 直接复用 q2 有风剖面 / 固定路径无风预检查 / 立即实现 collocation | 先执行固定路径无风/有风预检查；无风固定路径不可行前不直接进入正式求解器 | `baseline_feasibility.csv` 显示无风固定路径终端质量 `61163.474 kg`，违反 `m>=62000 kg`；需先重构可行初值或调整航程/约束口径 | q3 求解器实现门槛 |
| 2026-07-07 | D010 | q3 review2 后的第一版数值实现口径 | 直接求最优 / 时间域 collocation / 航程域可行性 Gate | 先实现航程域无风可行性 Gate，质量下限作为终端不等式；第一版不加严格控制变化率约束 | review2 指出固定路径不可行不能证明整体不可行，需先求最小松弛 `s*`；航程固定时用 `x` 作独立变量更稳定 | q3 可行性求解 |
| 2026-07-07 | D011 | q3 review3 后下一阶段任务 | 继续补理论文档 / 直接求无风最优 / 完整 collocation 可行性 NLP | 下一阶段只做完整航程域 collocation 可行性 NLP，先判断 `s*` 能否降到 0 | Gate 1 质量缺口仅 `10.214 kg`，但高度上界几乎激活，需用完整配点和边界敏感性确认可行性 | q3 Gate 2 |
| 2026-07-07 | D012 | q3 Gate 2 前技术口径 | 仅加密节点 / 完整配点+高度敏感性 / 先求有风最优 | Gate 2 必须同步包含完整航程域 collocation、`h_max` 敏感性、11 km C1 平滑大气和航程域 KKT 诊断 | review3(2) 指出 Gate 1 贴住 `h_max=12000 m`，且航程域乘子不能直接用于时间域 PMP | q3 Gate 2 |
| 2026-07-07 | D013 | q3 review4 后 Gate 2 实现入口 | 继续扩写方案 / 直接求最优 / 先实现 dry-run 投影和诊断链 | 先实现 C1 静力一致大气、Gate 1 warm start 投影、尺度化诊断和 manifest 入口；不把 dry-run 写成可行解 | review4 指出平滑大气、PMP 横截条件、约束尺度和 warm start 投影必须先明确；完整优化仍需后续 NLP 求解器 | q3 Gate 2 readiness |

| 2026-07-07 | D014 | q3 review5 后 Gate 2 dry-run 离散和证据命名 | 立即切换 Hermite-Simpson / 保持梯形 dry-run 并补诊断 / 直接实现完整 NLP | 保持当前航程域梯形 dry-run，配置写入 `collocation_transcription: trapezoidal`；新增 `warm_start_hmax_diagnostic.csv`、投影审计和 C1 大气诊断 | review5 要求冻结当前离散格式并避免把 warm-start hmax 表误写为 optimized sensitivity；完整 NLP 仍需单独实现 | q3 Gate 2 readiness |
| 2026-07-07 | D015 | q3 review6 后 C1 大气耦合和 hmax manifest 命名 | 继续只写文档 / 新增代码级耦合诊断并收紧 manifest 命名 / 直接进入非 dry-run NLP | 新增 `atmosphere_coupling_diagnostics.csv` 和有限差分静力残差；manifest 旧 `hmax_sensitivity` 输出入口改为 `legacy_warm_start_hmax_diagnostic`，正式敏感性统一预留 `optimized_hmax_sensitivity` | review6 指出 B 到 C 质量差为 0 需要代码级核查，且旧 manifest 名称可能被论文或汇总脚本误读为优化敏感性 | q3 Gate 2 readiness |
| 2026-07-07 | D016 | q3 review7 后正式 Gate 2 验证口径 | 继续增加 dry-run 文档 / 补 required-thrust 与静力步长诊断后进入非 dry-run NLP / 直接宣称 Gate 2 通过 | 补充 required-thrust 燃油耦合诊断、无量纲静力残差步长敏感性，并在配置中冻结重积分诊断、线性中点加事后重构检查和第二阶段硬质量约束策略 | review7 指出固定推力诊断尚不能证明燃油链路，配点缺陷不能替代连续 ODE 重积分，词典序第二阶段不能牺牲硬终端质量 | q3 Gate 2 readiness |
| 2026-07-07 | D017 | q3 review8 后非 dry-run Gate 2 输出与通过判定 | 覆盖 dry-run 表 / 独立 formal 表并纳入重积分判定 / 直接进入最终燃油优化 | 新增 `no_wind_collocation_formal_gate.csv` 和 `no_wind_collocation_formal_trajectory.csv`，保留 dry-run 表；Gate 通过判定必须包含独立重积分终端质量、高度和速度误差 | `N=31` 离散 NLP 已使质量松弛和配点缺陷接近零，但重积分误差尚未通过，不能写成严格可行或最优 | q3 Gate 2 NLP |
| 2026-07-07 | D018 | q3 review9 后 Gate 2 状态命名和连续诊断 | 保持 `needs_relaxation` / 改为离散可行但重积分失败 / 直接放宽门槛 | 改为 `discrete_feasible_reintegration_failed`，并补充分段线性控制重构、重积分有符号终端误差和每行 `h_max` 连续诊断 | 当前质量松弛为数值零，不是需要继续放松质量约束；失败来自连续 ODE 重积分速度误差和网格/重构一致性 | q3 Gate 2 NLP |
| 2026-07-07 | D019 | q3 review10 后是否进入最终无风燃油优化 | 进入最终燃油优化 / 先做 `N=31/61/121` 网格收敛 / 放宽重积分速度门槛 | 先做基准 `h_max=12000 m` 的 `N=31/61/121` 网格收敛，不放宽 `1e-3 m/s` 速度门槛，暂不进入最终燃油优化 | 误差比约 4，说明重积分误差随网格加密下降，但 `N=121` 速度误差仍为 `0.001897 m/s`，未过 Gate 2 门槛 | q3 Gate 2 NLP |
| 2026-07-07 | D020 | q3 review11 后是否进入最终无风燃油优化实现 | 继续 Stage 1B / 运行 `N=241` 后进入燃油目标 / 放宽门槛 | `N=241` 通过连续重积分、ODE 容差和沿程约束审计后，进入最终无风燃油优化实现；不把 Stage 1 可行轨迹写成最终最优 | `N=241` 速度误差约 `4.806e-4 m/s`，低于 `1e-3 m/s`；ODE 容差相邻速度差异最大约 `3.22e-6 m/s`；连续约束违反为 `0` | q3 Gate 2 -> final fuel optimization |
| 2026-07-08 | D021 | q3 review12 后是否把 Stage 1 可行轨迹写成最终结果 | 写成最优 / 只判定 Gate 2 通过并进入下一阶段 / 继续做可行性网格 | 只判定 Gate 2 可行性正式通过；最终无风燃油优化必须另以 `max m_f` 或 `min(m0-m_f)` 求解，并保留重积分、燃油恒等式、网格目标收敛、多初值、时间约束和零推力诊断 | review12 明确当前轨迹来自 `min s`，目标退化后只是任意可行轨迹；不能作为最优高度/速度/油耗结论 | q3 final fuel optimization |
| 2026-07-08 | D022 | q3 review13 后 `optimized_hmax_sensitivity` 的解释边界 | 作为最终燃油敏感性 / 作为 Gate 2 可行性敏感性 / 删除该表 | 保留该表，但明确它只支持 Gate 2 Stage 1 可行性；最终燃油最优下的 `h_max` 敏感性需在燃油目标求解后重新运行 | 当前表来自 `min s` 可行性目标，不来自 `min(m0-m_f)`；直接用于最终油耗结论会混淆可行性和最优性 | q3 evidence boundary |
| 2026-07-08 | D023 | q3 review14 后最终无风燃油优化验收边界 | 只报告最优 `m_f` / 运行后再定阈值 / 预先固定验收表 | 预先固定验收表：重积分速度 `<=1e-3 m/s`、高度 `<=0.1 m`、燃油恒等式 `<=0.05 kg`、连续约束 `<=1e-6`、`abs(J_121-J_241)<=1 kg` 且相对变化 `<=1e-4`、多初值目标差 `<=1 kg`；近零推力和 `tf/t_base` 报告并做时间约束对照 | review14 指出下一阶段不能只输出 `m_f`；目标收敛和多初值阈值必须在运行前固定，才能避免事后选择性解释 | q3 final fuel optimization |
| 2026-07-08 | D024 | q3 最终燃油全量 NLP 超时后采用何种正式数值分支 | 继续全变量 SLSQP / 降维连续射击 / 等待稀疏 NLP 工具链 | 采用 reduced-control continuous shooting 作为当前无风最终燃油优化主数值分支，推力和航迹角用 9 个分段线性控制结点，状态由航程域 ODE 连续积分 | 全变量 SLSQP 在 `N=61/241` 下超时；射击分支可在 `N=61->121->241` continuation 下通过 q3-T08 验收，且保留重积分、燃油恒等式、网格目标收敛和多初值诊断 | q3 final fuel optimization |
| 2026-07-08 | D025 | q3 最终燃油下 `h_max` 敏感性如何推进 | 直接跑 `N=241` 四档 / 先跑 `N=121` 局部重优化 / 只保留 Gate 2 可行性敏感性 | 先生成 `N=121`、单初值、四档 `h_max` 的最终燃油局部重优化敏感性表，作为阶段性证据；不把失败行或低网格结果写成强论文结论 | 四档 `N=241` 预计耗时较长；`N=121` 表已能显示高度上界放宽的燃油趋势和低高度上界风险，同时保留 `sensitivity_status` 防止过度解释 | q3 sensitivity |
| 2026-07-08 | D026 | q3 最终燃油下怠速推力敏感性如何推进 | 直接加入基础油耗项 / 先跑 `T_min` 局部重优化 / 只报告近零推力比例 | 先生成 `N=121`、单初值、`T_min/T_max={0,0.05,0.10}` 的最终燃油局部重优化敏感性表；基础油耗项作为后续扩展 | q3-T08 已报告近零推力比例为 0，但仍需验证提高推力下界是否改变局部最优；当前三档怠速边界均未激活，说明当前局部解不依赖零推力滑翔 | q3 sensitivity |
| 2026-07-08 | D027 | q4 策略对比口径 | 只固定航程比较油耗 / 只固定燃油比较航程 / 固定航程为主且固定燃油为辅 | 固定 q2 共同可行航程比较油耗节省，另以固定燃油或终止质量口径报告航程变化 | 题面同时要求“燃油节省比例”和“航程变化”；单一口径会牺牲另一指标的公平性。该口径与 q3 固定航程最优控制兼容，并能解释航程变化 | q4 design |
| 2026-07-08 | D028 | q4-T02/q4-T03 首轮实现的求解分支和主张强度 | 等待 q3 有风 continuation / q4 内部 reduced-control shooting / 全变量稀疏 NLP | q4 首轮采用内部有风 reduced-control shooting：从 q3 无风最终轨迹 warm start，优化推力和航迹角分段线性控制结点，输出 q4-T02/q4-T03；主张限定为局部降维射击解 | 该分支能复用已通过验收的 q3 无风结果并在当前工具链内可复现；但控制维度有限，不能支持全局最优表述，后续需 `beta` 重求解、多初值/控制结点和稀疏 NLP 交叉验证 | q4 implementation |
| 2026-07-08 | D029 | q4-T04/T06 和扩展框架的证据强度 | 强最优性敏感性和严格最大航程 / 局部有限主张 / 继续阻断等待更大求解器 | 采用局部有限主张收口：`beta` 五档逐场景重求解但非标称行只作为局部可行敏感性；固定燃油航程只报告未括住不可行上界前的下界估计；扩展框架标为 `framework_only` | 当前产物已经覆盖题面要求并可复现，但非标称 `beta` 场景未成功终止、固定燃油航程仍有余量，强结论需要后续更高迭代预算、多初值或稀疏 NLP 交叉验证 | q4 evidence boundary |
